# pacman::p_load(utf8, crayon)

options(prompt='wrappedtools: ')
options(continue='tell me more: ')
if (interactive()) {
  suppressMessages(require(devtools))
}
options(
  usethis.full_name = "Andreas Busjahn",
  usethis.description = list(
    `Authors@R` = 'person("Andreas", "Busjahn", email = "andreas@busjahn.net", 
    role = c("aut", "cre"), 
    comment = c(ORCID = "0000-0001-9650-6919"))',
    License =  "GPL-3",
    Version = "0.0.0.9000"
  ),
  usethis.protocol  = "ssh"  
)
# cranlogs::cran_downloads('wrappedtools',when = 'last-month')
# dplyr::summarize(cranlogs::cran_downloads('wrappedtools',
#                                           from = '2021-01-01'),
#                  total=sum(count))
